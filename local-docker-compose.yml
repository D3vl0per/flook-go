version: "3.7"
services:
  flook-go:
    build: .
    restart: unless-stopped
    environment:
      - IRC_CHANNEL=
      - IRC_SERVER=
      - IRC_NICK=flook-go
      - IRC_USER=flook-go
      - OPENAI_INSTANCE=
      - OPENAI_API_TOKEN=
      - OPENAI_MAX_TOKEN=
